'use strict';

const username = 'freddy';
export const typeString = typeof username === 'string';

console.log('hello word!');

const app = (a: number, b: number): number => {
  return a + b;
};

app(11, 2);
